sudo: required
services:
    - docker

before_install:
    - docker build -t mateuszbyra/react-test -f ./client/Dockerfile.dev ./client

script:
    - docker run -e CI=true mateuszbyra/react-test npm test

after_success:
    - docker build -t mateuszbyra/multi-client ./client
    - docker build -t mateuszbyra/multi-nginx ./nginx
    - docker build -t mateuszbyra/multi-server ./server
    - docker build -t mateuszbyra/multi-worker ./worker
    # Log in to the docker CLI
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
    # Take those images and push them to docker hub
    - docker push mateuszbyra/multi-client
    - docker push mateuszbyra/multi-nginx
    - docker push mateuszbyra/multi-server
    - docker push mateuszbyra/multi-worker